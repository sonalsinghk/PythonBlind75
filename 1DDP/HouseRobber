1. DP (TOP DOWN) : T.C. - O(N), S.C. - O(N)

APPROACH: calculating max of (dfs(1) + nums[0] + dfs(2)) that means comparing 1 with other elements but comparing max with 
(nums[0] + DFS(i+2) thats after leaving one block in between.

class Solution:
    def rob(self, nums: List[int]) -> int:

        memo = [-1] * len(nums)

        def dfs(i):
            if i >= len(nums):
                return 0
            if memo[i] != -1:
                return memo[i]
            memo[i] = max(dfs(i+1), nums[i] + dfs(i+2))
            return memo[i]

        return dfs(0)
                
2. DP (BOTTOM UP): T.C. - O(N), S.C. - O(N)

class Solution:
    def rob(self, nums: List[int]) -> int:

        if not nums:
            return 0
        if len(nums) == 1:
            return nums[0]
        
        dp = [-1] * len(nums)
        dp[0] = nums[0]
        dp[1] = max(nums[0],nums[1])

        for i in range(2,len(nums)):
            dp[i] = max(dp[i-1] , nums[i] + dp[i-2])

        return dp[-1]         #dp[n-1]

3. DP (SPACE OPTIMISED) : T.C. - O(N), S.C. - O(1)
class Solution:
    def rob(self, nums: List[int]) -> int:

        ROB1, ROB2 = 0,0

        for num in nums:
            temp = max(num+ROB1 , ROB2)
            ROB1 = ROB2
            ROB2 = temp
        
        return ROB2
        

        
        
